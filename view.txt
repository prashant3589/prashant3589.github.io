- task: PowerShell@2
  displayName: "Cleanup sources"
  condition: always()
  inputs:
    targetType: 'inline'
    script: |
      Remove-Item -Recurse -Force "$(Build.SourcesDirectory)\*" -ErrorAction SilentlyContinue
