# Path to your JSON file
$jsonPath = "C:\path\to\file.json"

# Read and encode
$jsonContent = Get-Content $jsonPath -Raw
$bytes = [System.Text.Encoding]::UTF8.GetBytes($jsonContent)
$base64 = [System.Convert]::ToBase64String($bytes)

# Output
$base64
