and(
  or(
    eq(root['result'][0]['state'],'Scheduled'),
    eq(root['result'][0]['state'],'Implement')
  ),
  eq(root['result'][0]['business_service']['display_value'],'PaymentsService'),
  greaterOrEquals(utcNow(), root['result'][0]['start_date']),
  lessOrEquals(utcNow(), root['result'][0]['end_date'])
)
