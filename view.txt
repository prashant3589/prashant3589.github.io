Write-Host "===== RECORDS ====="

foreach ($record in $response.records) {
    Write-Host "--------------------------------"
    Write-Host "Record Name: $($record.name)"
    Write-Host "Record Type: $($record.type)"
    Write-Host "Record State: $($record.state)"
    Write-Host "Record Comment: $($record.comment)"
}
