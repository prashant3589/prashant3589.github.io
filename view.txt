$user = "USERNAME"
Get-LocalGroup | ForEach-Object {
    if (Get-LocalGroupMember -Group $_.Name -ErrorAction SilentlyContinue | Where-Object { $_.Name -like "*$user*" }) {
        Write-Output "$user is a member of $($_.Name)"
    }
}
